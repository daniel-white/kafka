[package]
name = "kafka-raft"
version = "0.1.0"
edition = "2021"

[dependencies]
kafka-common = { path = "../kafka-common" }
kafka-protocol = { path = "../kafka-protocol" }
getset = "0.1"

[lints.rust]
unsafe_code = "deny"

[dev-dependencies]
rstest = "0.22"
