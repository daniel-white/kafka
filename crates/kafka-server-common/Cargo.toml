[package]
name = "kafka-server-common"
version = "0.1.0"
edition = "2021"

[dependencies]
kafka-common = { path = "../kafka-common" }
getset = "0.1"

[lints.rust]
unsafe_code = "deny"

[dev-dependencies]
rstest = "0.22"
